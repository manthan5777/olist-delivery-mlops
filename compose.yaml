services:
  api:
    build:
      context: .
      dockerfile: Dockerfile

    image: olist-delivery-api:v1

    container_name: olist-delivery-api

    ports:
      - "8001:8000"

    restart: unless-stopped